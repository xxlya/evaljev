# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Claude, via the Vector Institute OpenAI-compatible proxy.
# Key starts with "vp_". Obtain from your Vector account.
VECTOR_API_KEY=vp_replace_me

# Optional: override the proxy endpoint.
# VECTOR_BASE_URL=https://proxy.vectorinstitute.ai/v1

# Gemini, direct from Google AI Studio (https://aistudio.google.com/apikey).
GEMINI_API_KEY=replace_me
